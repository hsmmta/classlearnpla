{
  "name": "study-community-app",
  "version": "1.0.0",
  "description": "班级学习社区 Android 壳 App（Capacitor）",
  "private": true,
  "scripts": {
    "build": "echo Static web served from remote server.url",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "android:debug": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^5.7.2"
  }
}
